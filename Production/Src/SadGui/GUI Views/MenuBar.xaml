<UserControl x:Class="SadCLGUI.GUI_Views.MenuBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Menu 
            Grid.Row="0"
            Grid.ColumnSpan="2"
            IsMainMenu="True"
            >
            <MenuItem Header="Target" InputGestureText="F2">
                <MenuItem Header="Load from File" Name="Open" Click="Open_Click" />
                <MenuItem Header="Load from Server" Opacity="0.5" IsEnabled="False"/>
                <Separator/>
                <MenuItem Header="Clear Targets"/>
            </MenuItem>
            <MenuItem Header="_Launcher">
                <MenuItem
                    Header="DreamCheeky"
                    IsCheckable="True"
                    />
                <MenuItem
                    Header="Mock"
                    IsCheckable="True"/>
            </MenuItem>
        </Menu>
        <Image Height="Auto" HorizontalAlignment="Left" Margin="152,82,0,0" Name="image1" Stretch="Uniform" VerticalAlignment="Top" Width="Auto" />
    </Grid>
</UserControl>
